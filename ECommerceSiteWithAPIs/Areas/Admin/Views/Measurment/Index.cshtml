@using ECommerceSiteWithAPIs.Models.Dtos
@model IEnumerable<MeasurmentDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="card shadow border-0">
    @* <div class="card-header bg-secondary bg-gradient ml-0 py-3">
    <div class="row">
    <div class="col-12 text-center">
    <h1 class="text-white">Products List</h1>
    </div>
    </div>
    </div> *@
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-area="Admin" asp-controller="Measurment" asp-action="Upsert" class="btn bg-gradient-dark mb-0"><i class="bi bi-plus-square"></i>Create Unit Or Size</a>
            </div>
        </div>
        <table id="tblProducts" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <td style="width: 33rem;">Name</td>
                    <td class="text-center">Type</td>
                    @* <td class="text-center">Order</td> *@
                    <td class="text-center">Status</td>
                    <td class="text-center">Actions</td>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Count() > 0)
                {

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="align-middle">@item?.Name</td>
                            <td class="text-center align-middle">
                                @if (item?.Type == "1")
                                {
                                    <span class="badge bg-gradient-dark">Unit</span>
                                }
                                else
                                {
                                    <span class="badge bg-gradient-dark">Size</span>
                                }
                            </td>
                            <td class="text-center align-middle">
                                @if (item?.Status == "0")
                                {
                                    <span class="badge bg-info">Active</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">InActive</span>
                                }
                            </td>
                            <td class="text-center align-middle">
                                <a asp-area="Admin" asp-controller="Measurment" asp-action="Upsert" asp-route-id="@item.Id" class="badge bg-dark btn-link text-white px-3 mb-0">
                                    <i class="fas fa-pencil-alt text-white me-2"></i>
                                    Edit
                                </a>
                                <a asp-area="Admin" asp-controller="Measurment" asp-action="Delete" asp-route-id="@item.Id" class="badge bg-danger btn-link text-white px-3 mb-0">
                                    <i class="far fa-trash-alt me-2"></i>
                                    Delete
                                </a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>